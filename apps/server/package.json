{
  "name": "@attra/server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch --env-file=../../.env src/index.ts",
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "start": "node --env-file=../../.env dist/apps/server/src/index.js",
    "test": "node --import tsx/esm --test-isolation=process --test 'src/**/*.test.ts'"
  },
  "dependencies": {
    "@attra/shared": "workspace:^",
    "fastify": "^5.8.2",
    "zod": "^4.3.6"
  }
}
